import java.sql.*;  // JDBC 관련 패키지 임포트

public class JdbcTest_01 {
	public static void main(String[] args) {
		 // 1️ MySQL 연결 정보
        String url = "jdbc:mysql://localhost:____/______________?serverTimezone=Asia/Seoul&useSSL=false&characterEncoding=utf8";
        String user = "____";  // 계정
        String pass = "____";  // ✅ 설치 시 입력했던 비번

        // 2️ 연결 시도
        try (Connection conn = DriverManager.getConnection(url, user, pass)) {
            System.out.println("✅ MySQL 연결 성공!");
          
            // 쿼리 실행: 실행할 SQL 문 작성하기 (조회)
            String sql = "SELECT ____, _______ FROM account ORDER BY id";
            PreparedStatement ps = conn.prepareStatement(sql);
            ResultSet rs = ps.executeQuery();

            // 3️ DB에서 꺼낸 데이터 한 줄씩 출력
            while (rs.next()) {
                String owner = rs.getString("____");
                int balance = rs.getInt("_______");
                System.out.println(owner + "님의 잔액: " + balance + "원");
            }
            
            // 4️ 연결 자동 종료
        } catch (SQLException e) {
            System.out.println("❌ 연결 실패: " + e.getMessage());
        }
	}

}
